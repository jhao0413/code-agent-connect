{
  "name": "code-agent-connect",
  "version": "0.3.0",
  "description": "A minimal Telegram bridge for local Claude, Codex, and Neovate CLIs",
  "type": "module",
  "files": [
    "dist"
  ],
  "bin": {
    "code-agent-connect": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node scripts/run-tests.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  }
}
